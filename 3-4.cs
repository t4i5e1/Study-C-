using System;

namespace Example
{
    class Program
    {
        static void Main(string[] args)
        {
            int math = 80;
            int eng = 66;
            int his = 95;

            float average = (math + eng + his) / 3.0f;
            Console.WriteLine(average);
        }
    }
}
